APPLE V8.5 — TIME DROPDOWNS

- New Flight / Edit Flight time fields now use Apple native scrolling dropdowns.
- Departure and arrival time use hour + minute selectors and keep backend HHMM storage.
- Flight duration fields use hour + minute selectors and keep H:MM storage.
- FSTD time uses the same picker.
- Styling matches the existing Filter → Period dropdown language.
